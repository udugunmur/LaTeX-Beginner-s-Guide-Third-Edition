% Compile with LuaLaTeX
\documentclass{article}
\usepackage{luacode}
\begin{luacode}
  function GCD(a,b)
    if b ~= 0 then
        return GCD(b, a % b)
    else
        return a
    end
  end
\end{luacode}
\begin{document}
The greatest common divisor of 96 \not\equiv 36 is
\directlua{tex.sprint(GCD(96,36))}.
\end{document}%                                                                                                                                                                                                                                                



((addr in 10.43.164.4) or (addr in 10.43.164.4)) and not ( rule eq 'WAN-HUB-PRE_approve-user-vendorconnect' ) and not ( rule eq 'FLEET-PRE_approve-user-vendorconnect' )